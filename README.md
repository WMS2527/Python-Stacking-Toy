# Python-Stacking-Toy
Prints a stack list of colored words, representing rings. Users can add and remove rings. The ring added/removed depends on which rings are currently in the stack. Has a separate input for exiting the program.
